version: '3.7'
services:
  mongodb:
    image: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db

  nodeapp:
    build:
      context: ./node-app
      dockerfile: ../Dockerfile
    ports:
      - "8080:8080"
    depends_on:
      - mongodb

volumes:
  mongodb_data:
